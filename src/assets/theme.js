import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#237c2c",
    },
    secondary: {
      main: "#0a3610",
    },
    discord: {
      main: "#5865F2",
      contrastText: '#fff'
    },
  },
});

export default theme;
